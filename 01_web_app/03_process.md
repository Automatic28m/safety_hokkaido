# Boundaries

The web app must not contain LLM API keys or make safety decisions. It displays backend output, identifies unavailable service responses clearly, and preserves the user's chosen language.

